# ============================================================
# Gestor Financiero - Frontend Dockerfile (DESARROLLO)
# React (Vite) con Hot-Reload
# ============================================================

FROM node:20-alpine AS dev

WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
